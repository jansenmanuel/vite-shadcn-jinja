import {
	AlertDialog,
	AlertDialogAction,
	AlertDialogCancel,
	AlertDialogContent,
	AlertDialogDescription,
	AlertDialogFooter,
	AlertDialogHeader,
	AlertDialogTitle,
	AlertDialogTrigger,
} from "@/components/ui/alert-dialog";

export function AlertDialogComponent() {
	return (
		<AlertDialog>
			<AlertDialogTrigger>{{ trigger }}</AlertDialogTrigger>
			<AlertDialogContent>
				<AlertDialogHeader>
					<AlertDialogTitle>{{ content.header.title }}</AlertDialogTitle>
					<AlertDialogDescription>
						{{ content.header.description }}
					</AlertDialogDescription>
				</AlertDialogHeader>
				<AlertDialogFooter>
					<AlertDialogCancel>{{ content.footer.cancel }}</AlertDialogCancel>
					<AlertDialogAction>{{ content.footer.action }}</AlertDialogAction>
				</AlertDialogFooter>
			</AlertDialogContent>
		</AlertDialog>
	);
}
