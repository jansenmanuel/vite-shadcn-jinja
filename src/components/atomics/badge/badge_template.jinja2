{%- set allowed_variants = ["default", "destructive", "outline", "secondary"] -%}
{%- set variant = badge.variant if badge.variant in allowed_variants else "default" -%}

import { Badge } from "@/components/ui/badge";

export function BadgeComponent() {
	return <Badge{% if variant != 'default' %} variant="{{ variant }}"{% endif %}>{{ badge.text }}</Badge>;
}
